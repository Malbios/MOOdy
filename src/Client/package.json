{
  "name": "moody-client",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "dotnet fable watch . -o output -s --run vite",
    "build": "dotnet fable . -o output && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "blockly": "^13.2.1",
    "monaco-editor": "^0.52.0"
  },
  "devDependencies": {
    "vite": "^8"
  }
}
